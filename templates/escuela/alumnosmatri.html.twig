{% extends 'base.html.twig' %}

{% block title %}{{ titulo }}{% endblock %}

{% block body %}
        <ul>
            {% for matricula in matri %}
                <li>{{ matricula.getAlumnoMatricula().username }}</li>
            {% endfor %}
        </ul>
{% endblock %}
